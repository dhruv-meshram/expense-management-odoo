/**
 * public/js/utils/config.js
 * Stores global configurations like API endpoints.
 */

// This will be useful when swapping to a real backend API
const CONFIG = {
    // Current setup uses mock data; replace this with your backend URL
    API_BASE_URL: 'http://localhost:8080/api/v1', 
};